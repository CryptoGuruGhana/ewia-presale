import Layout from '../components/Layout';

const Dashboard = () => {
  return (
    <Layout>
      <h1 className="text-4xl font-bold">Welcome to my website!</h1>
<p className="mt-4 text-xl">
This is an example of a TypeScript Next.js web app with a responsive sticky navbar that changes background and text color when scrolling down, built using Tailwind CSS.
</p>
</Layout>
);
};

export default Dashboard;
